function firstLetter (elements, character) {
    var bNames= "";

    for (var i = 0; i < elements.length; i += 1) {
        if (elements[1][0] === charset) {
            bNames = elements[i] + " " + bNames;
        } else {

        }
    }
    return bNames.trim();
}